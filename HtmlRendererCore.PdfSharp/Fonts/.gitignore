# Ignore font files to prevent large binary files in repository
# Remove these lines once fonts are properly added and ready to commit
*.ttf
*.otf

# Keep the README and .gitkeep
!README.md
!.gitkeep
